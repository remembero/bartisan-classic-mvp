name: bartisan_classic_mvp
description: MVP for Bartisan Classic app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=2.19.0 \<3.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.0.0
  flutter_riverpod: ^2.0.0
  go_router: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
